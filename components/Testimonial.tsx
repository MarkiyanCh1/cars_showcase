import { TestimonialSlider } from ".";

export default function Testimonial() {
  return (
    <section
      className="max-width h-[450px] my-20 flex items-center"
      id="testimonial"
    >
      <div className="container h-[450px] mx-auto">
        <TestimonialSlider />
      </div>
    </section>
  );
}
